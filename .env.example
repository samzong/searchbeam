# YouTube API密钥 (必填)
# 从 https://console.developers.google.com/ 获取
YOUTUBE_API_KEY=your_youtube_api_key

# 认证Token列表，多个Token用逗号分隔 (必填)
AUTH_TOKENS=token1,token2,token3

# 缓存配置
# 缓存最大条目数
CACHE_MAX_ITEMS=1000
# 缓存过期时间(毫秒)，默认10分钟
CACHE_TTL=600000

# 服务器配置
PORT=3000
# 允许的跨域域名，多个域名用逗号分隔，设置为 * 表示允许所有域名
CORS_ORIGIN=*

# 分页配置
DEFAULT_PAGE_SIZE=10
MAX_PAGE_SIZE=50
